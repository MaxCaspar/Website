# The Codemap

Lookup-Table into our project

| File | Purpose |
| --- | --- |
| `specs/theBRIEF.md` | High-level product brief capturing target users, constraints, and v1 non-goals |
| `.beads/` | Task tracker state and issue queue (`issues.jsonl` is canonical; `beads.db` is the local query index rebuilt from JSONL via `br sync --import-only`) |
| `openspec/changes/maxcasper-vanilla-ascii-v1/specs/project-cards/spec.md` | Normative requirements for image-first project cards, desktop hover/focus metadata reveal, and mobile always-visible metadata |
| `openspec/changes/maxcasper-vanilla-ascii-v1/tasks.md` | OpenSpec implementation checklist mapped to tracker IDs for the MaxCasper v1 change |
| `specs/theCODEMAP.md` | High-level map of important files |
| `AGENTS.md` | Short repo learnings and rules |
| `index.html` | Static site shell with layered ASCII background, a top-of-page semantic primary nav, a focus-visible skip link targeting `#main-content`, an animated ASCII logo header mount above the Projects heading (`.projects-logo-shell` / `[data-projects-logo]`), retained section render roots, plus Google Fonts preconnect/stylesheet loading for Space Grotesk |
| `styles.css` | Global tmux-inspired gray/blue design tokens, Space Grotesk as the primary body/display UI font stack (with robust system sans fallbacks), explicit full-viewport ASCII layer geometry (`100vw` + `100vh/100dvh`) with 30%-larger baseline/mobile glyph sizing, skip-link/nav focus treatment, new organic round projects-logo glass/filter styling (`backdrop-filter` + `mix-blend-mode` + animated sheen with reduced-motion fallback), static two-column project card layout rules, dark glassmorphism treatment on `.project-meta` (blur/translucency + subtle refraction edge effects with `@supports` fallback), and compact writing-list/empty-state styling |
| `app.js` | ASCII terrain bootstrap plus content pipelines: runs scalar-field terrain mapping with layered macro-wave bands, localized shift-field vector distortion for regional morph/drift variation, and drift/bubble/pulse modulation tuned for subtle ambient motion under the tmux gray/blue palette (lower base alpha, softer pulse coupling, calmer bubbles), with intermittent tmux-tone bloom color blending on the ASCII layer (`#74c7ec`/`#89b4fa`/`#b4befe`) governed by explicit duration/amplitude/delay/alpha guardrail constants (including longer delays and lower bloom intensity from `px-eg7.3`), adds procedural organic round ASCII logo frame generation/animation above Projects with reduced-motion + hidden-tab-safe updates, uses viewport-aware sizing (`visualViewport` fallback to `innerWidth/innerHeight`) with reduced-motion + hidden-tab throttling, keeps an inline project seed model (pointing at committed `assets/projects/*.svg` previews), validates/normalizes project and writing entries, deterministically orders writing entries (date/title/path/slug), applies deterministic project size variants (featured first, compact every third, otherwise standard), loads markdown-entry manifests from `content/writing/index.json`, and renders escaped Projects/Writing views with fixed project media/content card markup (no overlay spans, `project-media` image column, `project-meta` content stack with title/slug/summary plus `project-meta-actions`) and explicit empty states |
| `assets/projects/*.svg` | Local project preview images consumed by the Projects cards to avoid broken external/missing asset paths |
| `content/writing/index.json` | Static writing manifest consumed by `app.js`; defines entry metadata (`title`, `date`, `excerpt`, `markdownPath`, optional `slug`) |
| `content/writing/*.md` | Markdown-backed writing entry source files referenced by the writing manifest |
| `scripts/validate-vanilla-runtime.sh` | CI-friendly guardrail that fails on framework runtime imports/script tags so production remains vanilla HTML/CSS/JS |
| `scripts/validate-readability.sh` | Readability guardrail that computes CSS-token contrast ratios and fails when panel text/link contrast drops below accessibility thresholds across baseline plus peak-bloom ASCII background states used in UX/a11y validation passes |
| `scripts/validate-performance-budget.sh` | Performance guardrail that enforces animation cadence/workload budgets plus bloom duration/amplitude/delay/alpha ceilings from runtime constants in `app.js` (desktop/mobile with reduced-motion and hidden-tab protections) |
| `scripts/validate-background-prominence.sh` | Background expression guardrail that verifies fullscreen coverage hooks, viewport-aware sizing, movement-prominence constants, and bloom presence expectations in runtime/CSS |
| `scripts/validate-layout-qa.sh` | Layout QA guardrail that verifies skip-link/nav semantics, focus-visible selectors, touch-target minimums, responsive non-overflow grid constraints, fixed desktop two-column project-card structure, no hover-only media transforms/overlays, long-text wrap resilience, image aspect-ratio reservation, and reduced-motion/visibility support hooks |
| `prompts/wateringGUIDE.md` | Single-iteration execution contract used by water-loop runs and drain automation tasks, including no-nested-loop and bounded-output guardrails |
| `.loops/watering/` | Water-loop run artifacts: per-iteration `.log` execution traces plus `.last.txt` sentinel captures (expected `WATERING-DONE`) used to investigate loop/drain failures; run03 documents a historical false-positive extra-iteration report later confirmed non-reproducible in current upstream `ralph-loop.sh` regressions |
